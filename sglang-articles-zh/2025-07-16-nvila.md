---
title: "如何在 SGLang 中支持新的视觉语言模型（VLM）：以 NVILA 为例"
title_en: "How to support new VLMs into SGLang: A Case Study with NVILA"
author: "The NVILA Team"
date: "July 16, 2025"
previewImg: /images/blog/nvila/preview.png
source: https://lmsys.org/blog/2025-07-16-nvila/
translated: 2026-09-12
---

# 如何在 SGLang 中支持新的视觉语言模型（VLM）：以 NVILA 为例

> 原文：[How to support new VLMs into SGLang: A Case Study with NVILA](https://lmsys.org/blog/2025-07-16-nvila/) · LMSYS Blog · The NVILA Team

大语言模型（LLM）的世界正以惊人的速度演进，视觉语言模型（VLM）站在这场变革的最前沿。这类模型支撑起了能够同时理解并推理图像与文本的应用。每天都有大量新的 VLM 模型涌现，我们希望把它们集成到 [SGLang](https://github.com/sgl-project/sglang) 中，以充分利用其高吞吐能力。今天，我们将以新近发布的 [NVILA 模型](https://arxiv.org/abs/2412.04468)为真实案例，逐步讲解如何将新的 VLM 集成到 SGLang 生态中。

## 用 SGLang 加速 NVILA 视觉语言模型

下面的基准测试对比了原始 VILA 实现与 SGLang 在不同并发度下的表现

![image.png](/images/blog/nvila/image.png)

在真实的 VLM 开发中，我们关注两个衡量推理服务系统性能的重要指标：**吞吐量（每秒 token 数，TPS）** 和**首 token 延迟（TTFT）**。

- 就 TPS 而言，更高的吞吐量意味着系统能同时生成更多 token。SGLang 的 `RadixAttention` 支持高效的请求批处理，显著提升了每秒生成的 token 数量。在并发为 8 时，SGLang 实现了超过 **4.4 倍的吞吐量**。
- 就 TTFT 而言，数值越低意味着用户越快收到首个 token 的响应。SGLang 的内存优化与高效 kernel 实现大幅降低了预填充（prefill）延迟。基准测试显示，并发为 8 时 SGLang 的响应最快可达 **2.2 倍**。

这些性能提升让 SGLang 成为在生产环境中部署 NVILA 这类高要求 VLM 的绝佳选择，现在 sglang 版本 ≥ 0.4.8 即可轻松部署。

```bash
python3 -m sglang.launch_server \
  --model-path Efficient-Large-Model/NVILA-Lite-2B-hf-0626 \
  --host 0.0.0.0 --port 30000 --trust-remote-code \
  --attention-backend fa3
```

## 全景概览：NVILA 这类 VLM 的工作原理

在深入代码之前，先了解底层发生了什么会很有帮助。大多数视觉语言模型（基于 llava 的模型）都共享一个三组件架构：

1. **视觉编码器（vision encoder）**——一个卷积网络，或更常见的视觉 Transformer（ViT），将像素转换为视觉 token 序列。
2. **投影器（projector）**——一个轻量级适配器（通常是 MLP），将这些 token 与预训练语言模型的嵌入空间对齐。
3. **token 处理器（token processor）**——即语言模型本身，它混合视觉与文本 token，并以自回归方式生成答案。

[NVILA 遵循这一范式](https://nvlabs.github.io/VILA)，同时凭借其 ***scale-then-compress***（先扩展后压缩）策略将效率前沿继续向前推进。它首先*扩展*（scale）空间与时间分辨率，让视觉塔（vision tower）能够看到高保真的图像或长视频；随后*压缩*（compress）得到的 token 流，使语言模型只需关注少量高信息量的 token：

- **高分辨率 SigLIP 视觉塔**捕获图像和多帧片段中的细粒度细节。
- **空间 token 池化**将稠密的 patch 缩减为小得多的网格，在保留文本与结构信息的同时削减了二次方复杂度的注意力开销。
- **时间池化**只保留信息量最大的帧，因此视频输入只会额外增加少量 token。
- **两层 MLP 投影器**将压缩后的视觉嵌入映射到 LLM 的嵌入空间。
- **FP8 训练、数据集剪枝与内存高效微调**最多可将训练成本削减 5 倍，并在单张 4090 GPU 上实现亚秒级预填充。

![NVILA 架构](https://arxiv.org/html/2412.04468v2/x5.png)

NVILA 架构

这一蓝图足够通用，可以覆盖大多数现代 VLM，而图中以绿色高亮的压缩模块正是让 NVILA 在规模化时既*准确*又*快速*的关键。

## 在 SGLang 中支持新模型

SGLang 为添加新模型提供了一套精简的流程，核心是创建一个利用 SGLang 核心组件的单一模型定义文件。关键在于用 SGLang 优化过的组件替换标准的 Hugging Face 组件。对于 VLM，这包括以下几个步骤。

## 第 1 步：将模型注册为多模态

首先，SGLang 需要把你的模型识别为多模态模型。这一步在 `sglang/srt/configs/model_config.py` 中处理。SGLang 通过检查模型的架构类名是否出现在 `multimodal_model_archs` 列表中来做出判断。

对于 NVILA，我们在该列表中添加 `"VILAForConditionalGeneration"`。这告诉 SGLang：任何具有此架构的模型都应被视为多模态模型。

```python
# python/sglang/srt/configs/model_config.py
# ... existing code ...
multimodal_model_archs = [
    # ... existing code ...    
    "VILAForConditionalGeneration",
]
# ... existing code ...
```

## 第 2 步：注册新的对话模板（chat template）

VLM 通常需要特定的对话模板来处理同时包含图像和文本的提示词。SGLang 既允许你定义新模板，也支持（更方便地）把你的模型匹配到已有模板上。

NVILA 使用类似 ChatML 的格式，因此我们可以复用现有的 `chatml` 对话模板。为了将模型与该模板关联起来，我们在 `python/sglang/srt/conversation.py` 中注册一个匹配函数。这个名为 `match_vila` 的函数会检查模型路径，找到匹配项时返回 `"chatml"`，从而告诉 SGLang 对 NVILA 模型应用 ChatML 格式。

```python
# python/sglang/srt/conversation.py
# ... existing code ...

@register_conv_template_matching_function
def match_vila(model_path: str):
    # ... existing code ...
    if re.search(r"vila", model_path, re.IGNORECASE):
        return "chatml"
```

这一注册告诉 SGLang 如何格式化输入字符串，以及图像数据应出现在何处。

为了更具体地理解这一点，我们来看看 `chatml` 模板提供的结构，以及它与 NVILA 需求的对应关系。

### 理解 NVILA 所用的 ChatML 模板

NVILA 使用的 ChatML 格式借助特殊 token 来组织对话结构。对于多模态提示词，该模板确保文本和图像都被正确放置。

一段典型的对话是消息的序列，每条消息带有 `role`（`system`、`user` 或 `assistant`）和 `content`。下面是带有图像的用户查询的格式化方式：

**用户消息示例：**

```json
{
  "role": "user",
  "content": [
    { "type": "text", "text": "What is in this image?" },
    { "type": "image" }
  ]
}
```

**生成的提示词字符串：**

模板处理器会将其转换为喂给模型的一个字符串：

```
<|im_start|>system
You are a helpful assistant.<|im_end|>
<|im_start|>user
What is in this image?<image><|im_end|>
<|im_start|>assistant
```

以下是各组成部分的说明：
- `<|im_start|>{role}` 和 `<|im_end|>`：这些 token 定义了每条消息的边界。
- 如果未提供系统提示词，会自动添加一个默认的系统提示词。
- 文本内容原样保留。
- 图像内容被替换为一个特殊的 `<image>` 占位 token。

通过将 NVILA 匹配到 `chatml` 模板，SGLang 会自动完成上述格式化。引擎知道在处理过程中把 `<image>` 占位符替换为实际的图像特征，这个过程我们将在第 3 步中介绍。

如果你的模型需要一个全新的对话模板，也可以直接在同一文件中定义。例如，假如 NVILA 使用了某种独特的格式，我们就需要创建一个新的对话模板：

```python
# python/sglang/srt/conversation.py
# ... existing code ...
register_conv_template(
    Conversation(
        name="vila",
        system_template="<|im_start|>system\\n{system_message}",
        system_message="You are a helpful assistant.",
        roles=("<|im_start|>user", "<|im_start|>assistant"),
        sep_style=SeparatorStyle.CHATML,
        sep="<|im_end|>",
        stop_str=["<|endoftext|>", "<|im_end|>"],
    )
)
```

## 第 3 步：构建多模态数据处理器

数据处理器是多模态集成的核心。它充当桥梁，把用户的原始输入——文本与图像的混合——精心处理成模型可用的形式。在 NVILA 集成中，我们在 `python/sglang/srt/managers/multimodal_processors/vila.py` 中创建了 `VILAMultimodalProcessor`。完整 diff 见[这里](https://github.com/sgl-project/sglang/pull/6106/files#diff-d9c481aa1e61e56644d3e89901895dafa9afd7e9d1147d48ab60c14435196bdb)。

该类继承自 SGLang 的 `BaseMultimodalProcessor`，其主力方法是 `process_mm_data_async`。我们逐步构建这个方法，看看它如何把原始数据转换为模型可用的格式。

### 处理器的骨架

我们先定义这个类。一个关键组件是 `models` 类属性，它将处理器与其对应的 SGLang 模型类关联起来。这一注册让 SGLang 在加载 `VILAForConditionalGeneration` 时能够识别该模型对应的处理器。`__init__` 方法也做了更新，直接从模型的配置中缓存 `image_token_id` 等特殊 token ID。处理器的主方法 `process_mm_data_async` 则接收原始请求信息，返回 SGLang 引擎能够理解的已处理数据。

```python
# python/sglang/srt/multimodal/processors/vila.py
# ... imports ...
class VILAMultimodalProcessor(BaseMultimodalProcessor):
    models: List[Type[nn.Module]] = [VILAForConditionalGeneration]

    # This holds the Hugging Face processor
    _processor: VILAProcessor

    def __init__(
        self,
        hf_config: PretrainedConfig,
        server_args: ServerArgs,
        _processor: VILAProcessor,
    ) -> None:
        super().__init__(hf_config, server_args, _processor)
        # Store special token IDs
        self.IM_TOKEN_ID = hf_config.image_token_id
        self.VIDEO_TOKEN_ID = hf_config.video_token_id

    async def process_mm_data_async(
        self,
        image_data: Optional[ImageDataItem | List[ImageDataItem]],
        input_text: str | List[int],
        request_obj: GenerateReqInput | EmbeddingReqInput,
        max_req_input_len: int,
        **kwargs,
    ) -> Optional[Dict[str, Any]]:
        # ... implementation below ...
```

### 从原始输入到处理后的数据

在 `process_mm_data_async` 内部，一条精简的流水线负责处理输入。

1. **加载与处理**：这一逻辑整合了数据处理流程。我们先调用基类的 `self.load_mm_data` 来加载图像文件，并定位提示词中的特殊 `<image>` token。随后将其输出传给 `self.process_and_combine_mm_data`，这个方法封装了文本分词、图像转换以及数据打包的逻辑。
2. **组装最终数据包**：这一过程直接生成供 SGLang 引擎使用的最终字典。该字典包含分词后的 `input_ids`、组装好的 `mm_items`（包括已处理的图像数据及其位置），并显式包含 `im_token_id` 和 `video_token_id`。

下面是 `process_mm_data_async` 的完整实现：

```python
# (Inside VILAMultimodalProcessor)
async def process_mm_data_async(
    self,
    image_data: Optional[ImageDataItem | List[ImageDataItem]],
    input_text: str | List[int],
    request_obj: GenerateReqInput | EmbeddingReqInput,
    max_req_input_len: int,
    **kwargs,
) -> Optional[Dict[str, Any]]:
    # Load image data and find special tokens
    base_output = self.load_mm_data(
        prompt=input_text,
        multimodal_tokens=MultimodalSpecialTokens(
            image_token=self._processor.tokenizer.image_token
        ),
        max_req_input_len=max_req_input_len,
        image_data=image_data,
    )

    # Process images and text, then combine into final format
    mm_items, input_ids = self.process_and_combine_mm_data(base_output)

    # Return the complete package for the engine
    return {
        "input_ids": input_ids.tolist(),
        "mm_items": mm_items,
        "im_token_id": self.IM_TOKEN_ID,
        "video_token_id": self.VIDEO_TOKEN_ID,
    }
```

就是这样！这一简洁的设计把核心处理逻辑集中在一起，使处理器更易维护、更易扩展。

## 第 4 步：创建核心模型定义

最后一个主要步骤是在新文件 `python/sglang/srt/models/vila.py` 中定义模型的主类。这一步要把 Hugging Face 模型移植过来，用 SGLang 的高性能等价实现替换标准层。

### 适配注意力机制

为了释放最大性能，标准注意力层被替换为：

- **语言模型注意力**：原始的 `Attention` 层被替换为 SGLang 的 `RadixAttention`。这是 SGLang 高性能批处理与内存管理的关键所在。
- **视觉模型注意力**：视觉 Transformer（ViT）中的 `Attention` 层被替换为 SGLang 的 `VisionAttention`，用于管理视觉塔的 KV 缓存。

NVILA 使用 Qwen2 作为其语言模型，因此我们直接复用 SGLang 的 `Qwen2ForCausalLM` 类，无需手动替换注意力层。diff 见[这里](https://github.com/sgl-project/sglang/pull/6106/files#diff-bd699d67711317806e08a0811bdf770194e95bee8b6ebe2d972ffa5e93c5a4e0)。

### 使用 `pad_input_ids` 处理多模态输入

`pad_input_ids` 函数是 SGLang 处理多模态输入的关键组件。它的主要职责是为 `RadixAttention` 准备输入 token 序列：将图像（或其他模态）的占位 token 替换为唯一的多模态数据哈希。这些哈希值（即 `pad_value`）让 `RadixAttention` 能够正确地将输入片段与对应的图像数据关联起来，即便在单个批次中同时处理多个请求也不会出错。这对高效的多用户服务至关重要。

SGLang 提供了不同的填充策略工具类，以适配模型表示多模态输入的各种方式。模式的选择取决于模型的分词器和对话模板的设计。

**模式 1：连续的多模态 token**

NVILA、Kimi-VL 等模型用一段连续的特殊 token 表示一张图像（例如 `<image><image>...<image>`）。对这类模型，我们使用 `MultiModalityDataPaddingPatternMultimodalTokens`。`pad_input_ids` 的函数签名经过泛化，可接受包含多种模态的 `mm_inputs`。对 NVILA 来说，实现十分简单直接：

```python
# python/sglang/srt/models/vila.py
# ...
def pad_input_ids(
    self,
    input_ids: List[int],
    mm_inputs: MultimodalInputs,
) -> List[int]:
    # Use a pattern that finds contiguous blocks of the image token
    pattern = MultiModalityDataPaddingPatternMultimodalTokens()
    return pattern.pad_input_tokens(input_ids, mm_inputs)
```

**模式 2：token 对**

InternVL、MiniCPM-o 等其他模型则用一对特殊 token 标记图像特征插入区域的起止（例如 `<image>...</image>`）。这种情况下应选择 `MultiModalityDataPaddingPatternTokenPairs`。该模式识别起止 token 之间的内容，并将其替换为图像的唯一哈希。

如果某个模型采用这种模式，实现大致如下：

```python
# python/sglang/srt/models/some_other_model.py
# ...    
def pad_input_ids(
    self,
    input_ids: List[int],
    image_inputs: MultimodalInputs,
) -> List[int]:
    # Use a pattern that finds regions enclosed by start/end token pairs     
    pattern = MultiModalityDataPaddingPatternTokenPairs(
        data_token_pairs=[
            (self.config.image_start_token_id, self.config.image_end_token_id)
        ],
    )
    return pattern.pad_input_tokens(input_ids, image_inputs)
```

选择合适的填充模式，就能确保 SGLang 引擎正确解读模型输入的多模态结构。

### 处理图像特征

输入序列完成填充后，SGLang 引擎需要获取实际的图像特征来替换占位哈希。这由 `get_image_feature` 方法负责。它的任务是把原始图像数据（`pixel_values`）转换为可与文本嵌入结合的嵌入表示。

VILA 的这一过程包含几个步骤：

1. `pixel_values` 被送入 `vision_tower`——一个预训练的视觉编码器（例如视觉 Transformer），从图像中提取丰富的特征表示。
2. 特征随后经过 `mm_projector`——一个将视觉特征与语言模型嵌入空间对齐的小型网络。得到的 `image_embedding` 即可供模型使用。

该函数由 `mm_utils.general_mm_embed_routine` 调用——这是 SGLang 的一个工具函数，负责在送入主语言模型之前，把占位哈希替换为计算得到的图像嵌入。

以下是 `python/sglang/srt/models/vila.py` 中的实现：

```python
# python/sglang/srt/models/vila.py    
def get_image_feature(self, mm_input: List[MultimodalDataItem]) -> Tensor:
    pixel_values = cast(Tensor, mm_input[0].pixel_values)
    vision_tower_output: BaseModelOutputWithPooling = self.vision_tower.__call__(
        pixel_values.to(
            device=self.vision_tower.device, dtype=self.vision_tower.dtype
        ),
        output_hidden_states=True,
    )
    mm_projector_input = self._vision_tower_output_to_mm_projector_input(
        vision_tower_output
    )
    image_embedding: Tensor = self.mm_projector.__call__(
        mm_projector_input.to(
            device=self.mm_projector.device, dtype=self.mm_projector.dtype
        )
    )
    return image_embedding
```

这种模块化设计确保图像特征得以计算并无缝融入语言模型的输入流。

### 定义 `forward` 前向传播

`forward` 方法经过适配以配合 SGLang 的批处理策略。它接收合并后的文本与图像，并在解码器层中借助 `RadixAttention` 完成处理。

```python
# python/sglang/srt/models/vila.py
def forward(
    self,
    input_ids: Tensor,
    positions: Tensor,
    forward_batch: ForwardBatch,
    get_embedding: bool = False,
) -> LogitsProcessorOutput:
    output = mm_utils.general_mm_embed_routine(
        input_ids=input_ids,
        forward_batch=forward_batch,
        language_model=self.llm,
        image_data_embedding_func=self.get_image_feature,
        get_embedding=get_embedding,
        positions=positions,
    )
    return cast(LogitsProcessorOutput, output)
```

### 实现 `load_weights`

由于 SGLang 使用定制优化的层，`load_weights` 函数负责仔细地映射（有时还需转换）Hugging Face 检查点中的权重，使其适配新的模型结构。这一过程高度依赖该模型在 Transformers 中的实现方式。

要增量加载权重，我们推荐使用子模块上的 `load_weights` 方法。其余权重则可使用 `weight_utils.default_weight_loader`。

```python
# python/sglang/srt/models/vila.py
def load_weights(self, weights: Iterable[Tuple[str, Tensor]]) -> None:
    params_dict = dict(self.named_parameters())
    for name, loaded_weight in weights:
        if name.startswith("llm."):
            self.llm.load_weights([(name[len("llm.") :], loaded_weight)])
        else:
            param = params_dict[name]
            weight_loader = getattr(
                param, "weight_loader", weight_utils.default_weight_loader
            )
            weight_loader(param, loaded_weight)
```

最后，在文件末尾添加 `EntryClass`，告诉 SGLang 服务器哪个类是该模型的主入口。

```python
# python/sglang/srt/models/vila.py
# ... model definition ...
# Entry class for the SGLang server
EntryClass = [VILAForConditionalGeneration]
```

## 第 5 步：添加集成测试

没有充分测试的集成是不完整的。最佳实践是通过两个关键方式验证新模型的实现。对于 NVILA，我们在 [`test/srt/test_vision_openai_server_b.py`](https://github.com/sgl-project/sglang/pull/6106/files#diff-40b31588286beebffb07cbcba6ac68278f65bfab723831352e4084b49d6ab84e) 中添加了一个测试用例。

## 结语

将 NVILA 这样的前沿 VLM 集成到 SGLang 这样的高性能推理服务引擎中，是一个细节繁多但路径清晰的过程。用 SGLang 的优化版本（如 `RadixAttention`）替换关键组件后，你就能以最高效率服务这些强大的模型，并解锁多用户批处理等高级特性。

关键步骤包括：

1. **配置**：注册模型的多模态属性及其对话模板。
2. **数据处理**：创建专用处理器来管理图像与文本输入。
3. **模型定义**：移植架构，用 SGLang 优化版本替换标准层，并正确处理多模态输入。
4. **测试**：对照参考输出严格验证实现，并添加集成测试。

希望这份详细的指南揭开了这一过程的神秘面纱，也鼓励你参与到 SGLang 令人兴奋的开源开发中来。

## 致谢

我们感谢所有贡献者为开发 NVILA 并将其集成到 SGLang 所付出的努力。

**NVILA 团队：** Zijian Zhang、Ligeng Zhu

**SGLang 社区：** Mick Qian、Xinyuan Tong、Qiujiang Chen、Xinpeng Wei、Chenyang Zhao

---

**延伸阅读：**

- [SGLang GitHub 仓库](https://github.com/sgl-project/sglang)
- [如何在 SGLang 中支持新模型](https://docs.sglang.io/supported_models/support_new_models.html)
- [NVILA 集成的 SGLang PR](https://github.com/sgl-project/sglang/pull/6106)
- [多模态处理器的 SGLang PR](https://github.com/sgl-project/sglang/pull/7629)
